services:
  postgres:
    image: postgres:16-alpine
    container_name: petcare-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: petcare
    ports:
      - "5433:5432"
    volumes:
      - petcare_data:/var/lib/postgresql/data

volumes:
  petcare_data:
